# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you: 
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: PyData STL
email: gauba.aayush@gmail.com
description: >- # this means to ignore newlines until "baseurl:"
  A community for developers and users of open source data tools.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://pydatastl.github.io" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: pydatastl
github_username:  pydatastl

# Set a path/url to a logo that will be displayed instead of the title
logo: "/assets/images/pydata.png"


# Google Analytics Tracking
ga_tracking: G-84WDCHVKH1
ga_tracking_anonymize_ip: true # Use GDPR compliant Google Analytics settings (true/nil by default)

# Aux links for the upper right navigation
aux_links: null

# Build settings
plugins:
  - jekyll-feed

# Preserve the response-header manifest for hosts that support Netlify-style
# _headers files. GitHub Pages itself does not apply custom response headers.
include:
  - _headers

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
